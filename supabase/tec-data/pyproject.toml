[project]
name = "pruebas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.31.0",
    "textual>=6.6.0",
    "typer>=0.15.0",
    "beautifulsoup4>=4.12.0",
]

[project.scripts]
tec-data = "src.main:app"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.14.10",
    "ty>=0.0.9",
]
