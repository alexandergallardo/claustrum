from __future__ import annotations

from pathlib import Path

PROJECT_ROOT = Path(__file__).resolve().parents[2]
ROOT = PROJECT_ROOT
WORKSPACE_ROOT = ROOT.parent.parent

DATA_ROOT = ROOT / "data"
STATE_ROOT = DATA_ROOT / "state"
RAW_ROOT = DATA_ROOT / "raw"
RAW_PROFESSORS_ROOT = RAW_ROOT / "professors"
RAW_CANDIDATES_ROOT = RAW_ROOT / "candidates"
PROCESSED_ROOT = DATA_ROOT / "processed"
PROCESSED_PROFESSORS_ROOT = PROCESSED_ROOT / "professors"
PROCESSED_REPORTS_ROOT = PROCESSED_ROOT / "reports"
REPORTS_ROOT = DATA_ROOT / "reports"
PROFESSOR_REPORTS_ROOT = REPORTS_ROOT / "professors"
DOWNLOAD_STATE_ROOT = STATE_ROOT / "downloads"
MATCHING_STATE_ROOT = STATE_ROOT / "matching"
COURSES_STATE_ROOT = STATE_ROOT / "courses"
MANUAL_MATCHES_ROOT = MATCHING_STATE_ROOT / "manual"
DOWNLOAD_INDEX_PATH = DOWNLOAD_STATE_ROOT / "index.json"
PROFESSORS_OUTPUT = MATCHING_STATE_ROOT / "professors.json"
UNMATCHED_OUTPUT = MATCHING_STATE_ROOT / "unmatched.json"
PROFESSOR_MATCH_DECISIONS_PATH = MATCHING_STATE_ROOT / "decisions.json"
COURSE_DECISIONS_PATH = COURSES_STATE_ROOT / "decisions.json"
COURSE_VARIATIONS_PATH = COURSES_STATE_ROOT / "variations.json"
COURSE_FAMILY_DECISIONS_PATH = COURSES_STATE_ROOT / "family-decisions.json"

# Backwards-compatible aliases used across the codebase.
CANDIDATE_RAW_ROOT = RAW_CANDIDATES_ROOT
PROCESSED_RAW_ROOT = PROCESSED_PROFESSORS_ROOT
