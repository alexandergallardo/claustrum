# -----------------------------------------------------------------------------
# Supabase - Environment Variables Template
#
# This repo uses a single Supabase base URL for the client app:
# - VITE_SUPABASE_URL
# plus a publishable (public) API key:
# - VITE_SUPABASE_PUBLISHABLE_KEY
#
# For privileged server-side operations (seed/admin), use:
# - SUPABASE_SECRET_KEY
#
# How to use:
# - Copy this file to `.env.local` for local dev
# - For production on Cloudflare, set these as Worker secrets/vars (not files)
# -----------------------------------------------------------------------------

# Supabase Project URL (base URL, NO /rest/v1 suffix)
# Local example:  http://127.0.0.1:54321
# Hosted example: https://<project-ref>.supabase.co
VITE_SUPABASE_URL=

# Publishable key (safe to expose publicly)
# Format: sb_publishable_...
VITE_SUPABASE_PUBLISHABLE_KEY=

# Secret key (SERVER-ONLY, elevated)
# Format: sb_secret_...
# DO NOT expose publicly. Do NOT embed in client code.
SUPABASE_SECRET_KEY=
